import { StepsProps } from "./types";
export declare function Steps({ children, data, onSubmit, hasProgress, hasNavigation, primaryColor, progressColor, navigationColor, item, mergedStep, mergedButtonsDisabled, mergedSetPrevStep, mergedSetNextStep }: StepsProps): import("react/jsx-runtime").JSX.Element;
