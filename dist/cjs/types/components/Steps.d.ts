import React from "react";
import { StepsProps } from "../types";
export declare function Steps({ children, onSubmit, hasProgress, hasNavigation, primaryColor, progressColor, navigationColor, item, defaultStep, defaultData, defaultSetPrevStep, defaultSetNextStep, defaultSetData }: StepsProps): React.JSX.Element;
