import React from "react";
import { StepsProps } from "../types";
export declare function MergeSteps({ children, onSubmit, hasProgress, hasNavigation, primaryColor, progressColor, navigationColor }: StepsProps): React.JSX.Element;
